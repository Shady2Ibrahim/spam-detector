# Spam Detector 🔍

A simple Flask web app to classify emails as 🚫 Spam or ✅ Not Spam using a machine learning model trained on real email data.

## How to Run Locally

1. Install dependencies:

```bash
pip install -r requirements.txt
